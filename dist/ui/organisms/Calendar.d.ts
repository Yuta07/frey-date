/// <reference types="react" />
import { AppProps } from '../../types';
export declare const Calendar: ({ registeredDates, selectedDates, handleDateClick, currentYear, currentMonth, onMovePreviousMonth, onMoveNextMonth, }: AppProps) => JSX.Element;
