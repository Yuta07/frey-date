/// <reference types="react" />
import { AppProps } from '../../types';
export declare const CalendarBody: ({ registeredDates, handleDateClick, currentYear, currentMonth, onMovePreviousMonth, onMoveNextMonth, }: AppProps) => JSX.Element;
