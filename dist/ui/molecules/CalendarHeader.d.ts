/// <reference types="react" />
import { CalendarProps } from '../../types';
export declare const CalendarHeader: ({ currentYear, currentMonth, onMovePreviousMonth, onMoveNextMonth }: CalendarProps) => JSX.Element;
